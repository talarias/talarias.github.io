name: Test

on: [push, pull_request]

jobs:

  build:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: test
      run:
        cd /web-ui
    - name: test install
      run:
        npm i